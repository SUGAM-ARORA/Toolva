---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: toolva-frontend-hpa
  namespace: toolva
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: toolva-frontend
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: toolva-backend-hpa
  namespace: toolva
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: toolva-backend
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
