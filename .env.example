# ─── Toolva Frontend Configuration ──────────────────────────
# This file contains ONLY public frontend configuration.
# No secrets, database credentials, or admin keys.

# Backend API URL (private backend service)
VITE_API_BASE_URL=http://localhost:8080/api

# Supabase Configuration (Optional - falls back to local data if empty)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# App Version
VITE_APP_VERSION=1.0.0
